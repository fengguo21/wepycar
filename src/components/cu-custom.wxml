<view class="cu-custom" wx:if="{{isShow}}">
  <view class='mask' bindtap="hide"></view>
  <view class="content">
    <view class="info">{{errInfo}}</view>
    <view class='btn' bindtap="hide">确定</view>
  </view>
</view>